/**
 * npm package entry point for @org-pulse/core server.
 *
 * Usage:
 *   const { startServer } = require('@org-pulse/core/server');
 *   startServer({ dataDir: '/path/to/data', modulePaths: [...] });
 */
module.exports = require('./dev-server');
